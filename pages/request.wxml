<!--pages/request.wxml-->

<view wx:for-items="{{result}}" wx:key="{{item.id}}">
    <view style="width:100%; height: 50rpx"></view>
    <!--index默认为下标，item为每项-->
    <text>第{{index}}条数据 {{item.abs}}</text>
     <image src="{{item.share_url}}" mode="aspectFill"></image>
</view>